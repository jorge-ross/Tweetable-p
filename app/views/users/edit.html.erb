<h1>Edit Profile</h1>

<%= render "form", user: @user %>

<br>

<div>
  <% link_to "Show this user", @user %>
  <%= button_to "Back to user", user_path, method: :get %>
</div>


<%= button_to 'Delete account', @user, method: :delete, data: { turbo_confirm: 'Are you sure?' } %>